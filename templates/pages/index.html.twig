{# templates/pages/index.html.twig #}
{% extends 'pages/layout.html.twig' %}

{% block title %}Accueil - Mon Site{% endblock %}

{% block content %}
    <h1>Bienvenue sur Mon Site</h1>
    <p>Page d'accueil en construction...</p>
    <a href="{{path('registration')}}">Compte</a>
    {% if registrationForm is defined %}
    {% include "pages/registration.html.twig" %}
    {% else %}
    <p>Formulaire non disponible</p>
    {% endif %}}
{% endblock %}
